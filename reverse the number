#include <stdio.h>

int main(){

    int Num=1234;
    int rev = 0;
    int rem=0;
    while (Num != 0){

        rem = Num % 10;

        rev = rev * 10 + rem;

        Num = Num/10;

    }    

    printf("The reversed number is: %d", rev);

    return 0;
}
